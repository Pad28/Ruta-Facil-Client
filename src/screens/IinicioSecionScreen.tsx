import { Text, View } from "react-native";
import { styles } from "../theme/appTheme";

export const IinicioSecionScreen = () => {
    return (
        <View style={{ marginTop: 20 }} >
            <Text style={styles.tiulo}>Pantalla de inicio de sesion</Text>
        </View>
    );
}
